from django.db import models

# Create your models here.


class slotModel(models.Model):
    catagory = models.
    location = 
    creator =
    nos =                                                                           #nos = number of slots
    avs =                                                                            #available slots
    time =
    date = models.DateTimeField
    status = models.BooleanField(default=False)